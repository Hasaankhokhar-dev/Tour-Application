import 'package:flutter/material.dart';
class Islamabad extends StatelessWidget {
  const Islamabad({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('islamabad');
  }
}
